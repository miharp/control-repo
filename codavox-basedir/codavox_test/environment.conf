# Minimal environment whose only job is to exercise codavox end to end.
# modulepath is relative, so it resolves inside whichever version directory a
# compiler has unpacked this into rather than pointing back at the primary.
modulepath = modules:$basemodulepath
