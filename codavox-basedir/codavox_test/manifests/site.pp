# Node definitions for the codavox test environment.
#
# Kept deliberately small: this environment exists to prove that codavox
# distributes a resolved tree and that a compiler serves catalogs from the exact
# version it reports, not to configure anything real.
node default {
  include codavox_demo
}

