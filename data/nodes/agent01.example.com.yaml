---
# OpenVox 9.0.0~rc1 agent (EL9 aarch64); this is the openvox9 branch.
# The openvox9 yum repo carries rc1 as of 2026-09-09, so the pin comes from the
# repo (it was installed from the artifact bucket while the repo lagged at
# beta2). Remove the pins once GA ships.
profile::base::release: 9
profile::openvox_agent::version: '9.0.0~rc1'
