---
# Pre-release test: OpenVox 9.0.0~beta1 agent from the new openvox9 repository.
# Note the ~ sorts OLDER than the previous alpha2 install, so applying this is
# a package downgrade (intentional — it lets the final 9.0.0 upgrade cleanly).
# Remove these pins once GA ships.
profile::base::release: 9
profile::openvox_agent::version: '9.0.0~beta1'
